version: 0.1

phases:
  pre_build: 
    commands:
      - echo Installing npm dependencies
      - npm install
  build: 
    commands:
      - echo Build Started
      - npm run build
artifacts:
  files:
    - build/*
  discard-paths: yes
    
